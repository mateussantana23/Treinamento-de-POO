class cat:
    def __init__(self, name, age, specie):
        self.name = name
        self.age = age
        self.specie = specie
        tim=cat("Tim", 4, "Persa")
    def __str__(self):
        return f"{self.name} is {self.age} years old, from {self.specie} specie"
    def speak(self, sound):
        return f"{self.name} says {sound}"
